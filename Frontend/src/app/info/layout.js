export const metadata = {
  title: 'Info | Semaphore 2K25',
  description: 'Information hub for Semaphore 2K25.'
};

export default function InfoLayout({ children }) {
  return (
    <section>
      {children}
    </section>
  );
}